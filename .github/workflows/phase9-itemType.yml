name: phase9-itemType
on:
  pull_request:
    paths:
      - "rules/2024/itemType.json"
      - "schema/2024/v1/itemType.schema.json"
      - "scripts/phase9/validate_itemType.py"
      - ".github/workflows/phase9-itemType.yml"
  push:
    branches: [ master ]
    paths:
      - "rules/2024/itemType.json"
      - "schema/2024/v1/itemType.schema.json"
      - "scripts/phase9/validate_itemType.py"
      - ".github/workflows/phase9-itemType.yml"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install deps
        # normalised: pinned-runtime
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
      - name: Validate itemType.json
        run: |
          python scripts/phase9/validate_itemType.py






