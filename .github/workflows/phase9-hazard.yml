name: phase9-hazard
on:
  pull_request:
    paths:
      - "rules/2024/hazard.json"
      - "schema/2024/v1/hazard.schema.json"
      - "scripts/phase9/validate_hazard.py"
      - ".github/workflows/phase9-hazard.yml"
  push:
    branches: [ master ]
    paths:
      - "rules/2024/hazard.json"
      - "schema/2024/v1/hazard.schema.json"
      - "scripts/phase9/validate_hazard.py"
      - ".github/workflows/phase9-hazard.yml"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install jsonschema
      - name: Validate hazard.json
        run: |
          python scripts/phase9/validate_hazard.py
