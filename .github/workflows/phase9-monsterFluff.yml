name: phase9-monsterFluff
on:
  pull_request:
    paths:
      - "rules/2024/monsterFluff.json"
      - "schema/2024/v1/monsterFluff.schema.json"
      - "scripts/phase9/validate_monsterFluff.py"
      - ".github/workflows/phase9-monsterFluff.yml"
  push:
    branches: [ master ]
    paths:
      - "rules/2024/monsterFluff.json"
      - "schema/2024/v1/monsterFluff.schema.json"
      - "scripts/phase9/validate_monsterFluff.py"
      - ".github/workflows/phase9-monsterFluff.yml"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install jsonschema
      - name: Validate monsterFluff.json
        run: |
          python scripts/phase9/validate_monsterFluff.py