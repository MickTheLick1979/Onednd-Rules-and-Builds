name: phase9-raceFluff
on:
  pull_request:
    paths:
      - "rules/2024/raceFluff.json"
      - "schema/2024/v1/raceFluff.schema.json"
      - "scripts/phase9/validate_raceFluff.py"
      - ".github/workflows/phase9-raceFluff.yml"
  push:
    branches: [ master ]
    paths:
      - "rules/2024/raceFluff.json"
      - "schema/2024/v1/raceFluff.schema.json"
      - "scripts/phase9/validate_raceFluff.py"
      - ".github/workflows/phase9-raceFluff.yml"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install jsonschema
      - name: Validate raceFluff.json
        run: |
          python scripts/phase9/validate_raceFluff.py
