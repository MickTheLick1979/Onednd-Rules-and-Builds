name: phase9-featFluff
on:
  pull_request:
    paths:
      - "rules/2024/featFluff.json"
      - "schema/2024/v1/featFluff.schema.json"
      - "scripts/phase9/validate_featFluff.py"
      - ".github/workflows/phase9-featFluff.yml"
  push:
    branches: [ master ]
    paths:
      - "rules/2024/featFluff.json"
      - "schema/2024/v1/featFluff.schema.json"
      - "scripts/phase9/validate_featFluff.py"
      - ".github/workflows/phase9-featFluff.yml"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install jsonschema
      - name: Validate featFluff.json
        run: |
          python scripts/phase9/validate_featFluff.py
