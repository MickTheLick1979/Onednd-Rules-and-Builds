name: phase9-speciesFeature
on:
  pull_request:
    paths:
      - "rules/2024/speciesFeature.json"
      - "schema/2024/v1/speciesFeature.schema.json"
      - "scripts/phase9/validate_speciesFeature.py"
      - ".github/workflows/phase9-speciesFeature.yml"
  push:
    branches: [ master ]
    paths:
      - "rules/2024/speciesFeature.json"
      - "schema/2024/v1/speciesFeature.schema.json"
      - "scripts/phase9/validate_speciesFeature.py"
      - ".github/workflows/phase9-speciesFeature.yml"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install jsonschema
      - name: Validate speciesFeature.json
        run: |
          python scripts/phase9/validate_speciesFeature.py
