name: phase9-species
on:
  pull_request:
    paths:
      - "rules/2024/species.json"
      - "schema/2024/v1/species.schema.json"
      - "scripts/phase9/validate_species.py"
      - ".github/workflows/phase9-species.yml"
  push:
    branches: [ master ]
    paths:
      - "rules/2024/species.json"
      - "schema/2024/v1/species.schema.json"
      - "scripts/phase9/validate_species.py"
      - ".github/workflows/phase9-species.yml"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install jsonschema
      - name: Validate species
        run: |
          python scripts/phase9/validate_species.py