name: phase9-status
on:
  pull_request:
    paths:
      - "rules/2024/status.json"
      - "schema/2024/v1/status.schema.json"
      - "scripts/phase9/validate_status.py"
      - ".github/workflows/phase9-status.yml"
  push:
    branches: [ master ]
    paths:
      - "rules/2024/status.json"
      - "schema/2024/v1/status.schema.json"
      - "scripts/phase9/validate_status.py"
      - ".github/workflows/phase9-status.yml"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install jsonschema
      - name: Validate status.json
        run: |
          python scripts/phase9/validate_status.py
