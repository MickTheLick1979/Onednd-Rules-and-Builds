name: phase9-legendaryGroup
on:
  pull_request:
    paths:
      - "rules/2024/legendaryGroup.json"
      - "schema/2024/v1/legendaryGroup.schema.json"
      - "scripts/phase9/validate_legendaryGroup.py"
      - ".github/workflows/phase9-legendaryGroup.yml"
  push:
    branches: [ master ]
    paths:
      - "rules/2024/legendaryGroup.json"
      - "schema/2024/v1/legendaryGroup.schema.json"
      - "scripts/phase9/validate_legendaryGroup.py"
      - ".github/workflows/phase9-legendaryGroup.yml"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install jsonschema
      - name: Validate legendaryGroup.json
        run: |
          python scripts/phase9/validate_legendaryGroup.py
