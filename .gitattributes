# --- Canonical line endings: LF; ensure text normalization ---
*           text=auto

*.{json,jsonc,json5}  text eol=lf
*.{yml,yaml}          text eol=lf
*.{py,ps1,psm1,psd1}  text eol=lf
*.md                  text eol=lf
*.txt                 text eol=lf

# Do not attempt CRLF for Windows just because it's Windows:
# we build on Linux runners; LF everywhere keeps diffs clean.
