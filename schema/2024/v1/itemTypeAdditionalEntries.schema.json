{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "itemTypeAdditionalEntries (v1)",
  "description": "Conservative v1 schema for 2024 itemTypeAdditionalEntries.",
  "anyOf": [
    { "type": "array", "items": { "$ref": "#/$defs/item" } },
    {
      "type": "object",
      "properties": {
        "itemTypeAdditionalEntries": {
          "type": "array",
          "items": { "$ref": "#/$defs/item" }
        }
      },
      "required": ["itemTypeAdditionalEntries"],
      "additionalProperties": true
    }
  ],
  "$defs": {
    "item": {
      "type": "object",
      "properties": {
        "name":   { "type": "string", "minLength": 1 },
        "source": { "type": "string" },
        "page":   { "type": ["integer","null"] },
        "entries": {
          "type": ["array","null"],
          "items": { "type": ["string","object","array","number","boolean","null"] }
        },
        "_copy": { "type": ["object","null"] }
      },
      "required": ["name"],
      "additionalProperties": true
    }
  }
}
