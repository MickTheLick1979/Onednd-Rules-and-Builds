{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "spellFluff (v1)",
  "description": "Conservative v1 schema for 2024 spellFluff.",
  "anyOf": [
    { "type": "array", "items": { "$ref": "#/$defs/item" } },
    {
      "type": "object",
      "properties": {
        "spellFluff": {
          "type": "array",
          "items": { "$ref": "#/$defs/item" }
        }
      },
      "required": ["spellFluff"],
      "additionalProperties": true
    }
  ],
  "$defs": {
    "entry": { "type": ["string","object","array","number","boolean","null"] },
    "image": { "type": ["string","object","null"] },
    "item": {
      "type": "object",
      "properties": {
        "name":   { "type": "string", "minLength": 1 },
        "source": { "type": "string" },

        "entries": {
          "type": ["array","null"],
          "items": { "$ref": "#/$defs/entry" }
        },
        "images": {
          "type": ["array","null"],
          "items": { "$ref": "#/$defs/image" }
        },

        "additionalSources": {
          "type": ["array","null"],
          "items": { "type": ["string","object","null"] }
        },
        "otherSources": {
          "type": ["array","null"],
          "items": { "type": ["string","object","null"] }
        },

        "_copy": { "type": ["object","null"] }
      },
      "required": ["name"],
      "additionalProperties": true
    }
  }
}
